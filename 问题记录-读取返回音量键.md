# 问题记录：读取返回 / 音量+ / 音量- 按键信号

> 状态：未解决
> 目标：在开启 Windows 内存完整性（HVCI）的环境下，可靠读取小米遥控器 RC003 的返回、音量+、音量- 三个按键信号。
> 背景：方向、OK、Home、Menu、Power、TV 等键可通过 Raw Input 正常获取；语音走 ATVV 正常。唯独这三个键被系统 HOGP 层过滤，普通应用无法直接收到。

---

## 1. 问题定义

### 1.1 按键通道

| 按键 | 通道 | 当前状态 |
| --- | --- | --- |
| 方向 / OK / Home / Menu / Power / TV | 标准 HID → Raw Input | ✅ 可获取 |
| 麦克风 / 语音 | ATVV GATT | ✅ 可获取 |
| 返回 | HOGP 非标准 usage（0x00F1） | ❌ 不可获取 |
| 音量+ | HOGP 非标准 usage（0x0080） | ❌ 不可获取 |
| 音量- | HOGP 非标准 usage（0x0081） | ❌ 不可获取 |

### 1.2 根本障碍

- RC003 的返回 / 音量键走 **HID-over-GATT（HOGP）**。
- Windows 的 `hidbthle.sys` 会先处理/过滤这些报告，普通应用通过 Raw Input / HID API 收到的信息已经被系统过滤。
- 在开启 **HVCI（内存完整性）** 的机器上，向 WUDFHost 注入 Frida Gadget 会被拦截，导致无法在系统过滤前截获报告。

---

## 2. 尝试过的方案

### 2.1 Frida HID Tap（注入 WUDFHost）—— 原始方案

**思路**
- 用 Frida Gadget 注入到 HOGP 宿主进程（WUDFHost.exe）。
- Hook `NtDeviceIoControlFile`，在系统处理前截获 9 字节 HOGP 报告。
- 解析 usage：`0x00F1`=返回、`0x0080`=音量+、`0x0081`=音量-。

**结果**
- ❌ 失败。
- 日志：`LoadLibraryW 返回空（Gadget 未被加载，可能被内存完整性拦截）`。
- 确认系统开启了 VBS/HVCI：`SecurityServicesRunning: 2`、`HVCI Enabled: 1`。
- 未签名第三方 DLL 注入被 HVCI 拦截。

**结论**
- 在 HVCI 开启的机器上，Frida 注入路径不可用。
- 普通键与语音不受影响，仅三个特殊键缺失。

---

### 2.2 自动下载 Frida Gadget

**思路**
- 让应用在缺少 Frida Gadget 时自动执行 `scripts/fetch-frida-gadget.ps1`，无需用户手动运行。

**结果**
- ✅ 自动下载本身可以工作（网络正常时）。
- ❌ 但下载成功后的**注入仍然被 HVCI 拦截**，没有解决根本问题。
- 该改动随后已撤销（工作区已清空）。

**结论**
- 自动下载只解决“缺少 Gadget”的问题，不解决“HVCI 拦截注入”的问题。

---

### 2.3 Raw Input fallback（借鉴 Voice_VibeCoding）

**思路**
- 参考 `Voice_VibeCoding` 的 `raw_mapping.rs`：
  - 用 `RegisterRawInputDevices` 监听键盘 Raw Input。
  - 将收到的 VK 映射为按键：
    - `0xA6`（166）→ 返回
    - `0xAF`（175）→ 音量+
    - `0xAE`（174）→ 音量-
- 在 `core-hid/src/raw_input.rs` 增加 `is_remote_special_vkey`，把这些 VK 从普通键盘过滤中放行。

**结果**
- ❌ 失败。
- 实测日志显示：
  - Raw Input 能收到方向（vkey=38/40/37/39）、确定（vkey=13）、F5（vkey=116）、电源（vkey=255）。
  - **收不到返回（166）、音量+（175）、音量-（174）**。
- 说明该机器上 Windows 根本没有把这些键翻译成标准 VK 送到 Raw Input。
- 该改动随后已撤销（工作区已清空）。

**结论**
- Raw Input fallback 在部分环境下可行（Voice_VibeCoding 作者环境），但在本机不可行。
- 原因是系统 HOGP 层过滤了这些非标准 usage，没有暴露成标准 VK。

---

### 2.4 WinUHid 方案调研

**思路**
- 参考 `Voice_VibeCoding` 的 WinUHid：安装 UMDF 虚拟 HID 驱动，绕过 HVCI 注入限制。

**结果**
- ❌ 方向错误。
- WinUHid 是**输出工具**：创建虚拟 HID 设备，用于向系统注入按键（如语音键快捷键）。
- 它**不能读取**真实物理设备（遥控器）发出的 HOGP 信号。
- 因此对“读取返回/音量键”没有直接帮助。

**结论**
- WinUHid 不能解决读取问题，只能用于“输出虚拟按键”。

---

### 2.5 HidHide 方案（讨论中）

**思路**
- HidHide 是内核态 HID 过滤驱动（设备防火墙）。
- 用 HidHide 将小米遥控器从系统中“隐藏”。
- 将应用加入白名单，使其独占打开设备。
- 应用直接读取原始 HID 报告，绕过系统驱动过滤。

**现状**
- ⏳ 尚未实测。
- 理论上：
  - 如果 HidHide 能把 HOGP 设备从 `hidbthle.sys` 手中“抢走”，系统驱动不处理，应用就可能读到完整原始报告。
  - 不依赖 Frida 注入，不受 HVCI 影响。
- 待验证：
  - HidHide 对 HID-over-GATT（HOGP）设备的兼容性。
  - 隐藏后应用能否通过 HID API 打开设备并读取报告。

---

## 3. 方案对比

| 方案 | 是否依赖注入 | 是否受 HVCI 影响 | 本机实测 | 结论 |
| --- | --- | --- | --- | --- |
| Frida HID Tap | 是 | 是 | ❌ 被拦截 | 不可用 |
| 自动下载 Frida | 是 | 是 | ❌ 注入仍被拦 | 不可用 |
| Raw Input fallback | 否 | 否 | ❌ 收不到 | 本机不可用 |
| WinUHid | 否 | 否 | 未实测 | 方向错误，无法读取 |
| HidHide + 独占读取 | 否 | 否 | 未实测 | 待验证 |

---

## 3.5 最新实验进展（2026-08-28）

### ✅ 好消息：HidHide 已成功隐藏 RC003 HID 设备

**经过实测确认：**

1. **HidHide 已安装并正常加载**
   - 服务：`Running`（Nefarius HidHide Service）
   - 驱动：`Running`（`C:\WINDOWS\system32\drivers\HidHide.sys`）
   - CLI 版本：`1.5.230.0`

2. **RC003 HID 设备实例已识别并成功隐藏**
   - 设备实例路径：
     ```
     HID\{00001812-0000-1000-8000-00805F9B34FB}_DEV_VID&012717_PID&32B8_REV&00A4_C05D39C36462\9&29512BC&0&0000
     ```
   - `HidHideCLI --dev-list` 确认该设备已加入隐藏列表。
   - 全局隐藏开关已开启（`--cloak-on`）。

3. **关键经验**
   - HidHide 的 `--dev-all` 输出会截断，不能依赖它来找设备。
   - 改用 `Get-PnpDevice` 直接查询 RC003 的 HID 设备实例路径更可靠。
   - HidHideCLI 查询/配置需要**管理员权限**，普通权限会返回 `Access Denied (0x5)`。

4. **安装与重启要求（重要）**
   - HidHide 需要**手动下载安装包并安装**，不是应用内自动安装。
   - 官方安装包：`HidHide_1.5.230_x64.exe`（GitHub Releases）。
   - 安装时以管理员身份运行，**安装完成后必须重启电脑**，驱动才会真正加载生效。
   - 重启前查询服务/驱动会显示未安装（`SERVICE: not found`、`DRIVER: not found`）。
   - 重启后确认：
     - 服务 `Nefarius HidHide Service` 状态为 `Running`
     - 驱动 `C:\WINDOWS\system32\drivers\HidHide.sys` 状态为 `Running`
     - `HidHideCLI.exe --version` 能正常输出

5. **设备实例路径会变化（重要）**
   - 小米遥控器在 Windows 中的“设备名 / 友好名”不是固定的，本次连接后叫 **`MI RC`**，但可能随配对/系统变化。
   - 更关键的是 **HID 设备实例路径（device instance path）不是固定的**：
     - 重新配对遥控器后，`InstanceId` 中的末尾部分（如 `9&29512BC&0&0000`）可能变化。
     - 因此之前 `--dev-hide` 保存的路径可能会失效，需要重新查询并隐藏。
   - 正确做法：
     - 每次实验前用 `Get-PnpDevice` 按 VID/PID（`VID_012717&PID_32B8`）或 HID GUID（`00001812`）重新查询最新实例路径。
     - 不要硬编码设备实例路径。

**如何确认“能隐藏”（完整操作记录）：**

步骤 1：查询 RC003 的 HID 设备实例路径（PowerShell）：
```powershell
Get-PnpDevice -PresentOnly | Where-Object {
  $_.InstanceId -match 'HID\\\{00001812' -and $_.InstanceId -match '2717|32B8'
} | Select-Object Status,Class,FriendlyName,InstanceId
```

步骤 2：用 HidHideCLI 隐藏该设备（需要管理员权限）：
```powershell
$cli = "$env:ProgramFiles\Nefarius Software Solutions\HidHide\x64\HidHideCLI.exe"
$devPath = 'HID\{00001812-0000-1000-8000-00805F9B34FB}_DEV_VID&012717_PID&32B8_REV&00A4_C05D39C36462\9&29512BC&0&0000'
& $cli --dev-hide "$devPath"
& $cli --cloak-on
```

步骤 3：确认隐藏成功（管理员权限）：
```powershell
& $cli --dev-list
# 输出应包含 --dev-hide "HID\{...}"
& $cli --cloak-state
# 输出应为 --cloak-on
```

**注意：**
- 普通权限运行 CLI 会报 `Error code 0x0005 ... 拒绝访问`，必须用管理员 PowerShell 执行。
- `--dev-list` / `--cloak-state` 返回空或报错时，先确认是否用管理员权限运行。

**当前状态：**
- ✅ 设备已隐藏
- ✅ 全局隐藏已开启
- ❌ 应用白名单尚未配置（下一步）
- ❌ HID API 读取测试尚未执行（下一步）

**待验证：**
- 把测试程序加入白名单后，能否通过 HID API 打开隐藏后的 RC003 设备。
- 能否读到返回（0x00F1）、音量+（0x0080）、音量-（0x0081）原始报告。

---

## 4. 下一步候选方案

1. **实测 HidHide + HID API**
   - 安装 HidHide，隐藏遥控器，白名单应用。
   - 用 `Windows.Devices.HumanInterfaceDevice.HidDevice` 或 `HidD_GetInputReport` 尝试读取。
2. **应用直接 BLE GATT 连接 HID 服务（0x1812）**
   - 纯用户态，不依赖系统 HID 驱动。
   - 风险：可能被系统独占或 `AccessDenied`。
3. **自写 UMDF 过滤驱动**
   - 用户态驱动不受 HVCI 影响，但工程量大。
4. **关闭 HVCI（不推荐）**
   - 最直接，但降低系统安全性。

---

## 5. 参考资料

- Voice_VibeCoding：`https://github.com/mwlt/Voice_VibeCoding`
  - HID Tap：Frida 注入 WUDFHost，与原始方案相同。
  - Raw Input fallback：`raw_mapping.rs`，部分环境可行。
  - WinUHid：仅用于虚拟键盘输出，不能读取。
- HidHide：`https://github.com/nefarius/HidHide`
  - 内核态 HID 过滤驱动，用于隐藏设备/白名单应用。
- Windows HOGP：HID-over-GATT 由 `hidbthle.sys` 处理。

---

## 6. 结论

- 当前确认不可用：Frida 注入（HVCI 拦截）、Raw Input fallback（本机收不到）。
- WinUHid 不能用于读取。
- 最有希望的下一步：**HidHide 隐藏 + HID API 独占读取**，或 **BLE GATT 直连 HID 服务**。
