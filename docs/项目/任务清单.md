# TODO — Windows 无线麦（Remote Mic）

> 关联文档：[规划.md](./规划.md)
> 技术栈：Rust 核心 + Tauri 2 / TypeScript / React
> 目标设备：小米蓝牙遥控器 2 Pro / RC003，后续可扩展 RC001

图例：
- `[ ]` 待办
- `[x]` 已完成

---

## 0. 前期调研与基线复验

- [x] 梳理 RC003/RC001 的公开蓝牙协议资料（ATVV、HID、ADPCM）
- [x] 整理 Windows BLE/HID/虚拟声卡实现路线的可行性资料
- [x] 整理 ATVV 协议关键事实（UUID、ADPCM、会话命令、HID usage）
- [x] 确认 GPL-3.0 / 第三方组件许可影响
- [x] 准备真实 RC003 遥控器与 Windows 10/11 x64 测试机
- [x] M0：用真机 RC003 完成“配对 → 按键 → 语音”链路行为采集（语音出字待补测）
- [x] 记录真机 HID 原始数据与语音链路日志，作为 Rust 实现对照

## 1. 工程初始化

- [x] 初始化 Git 仓库并推送到 `windows-remote-mic`
- [x] 创建 Rust workspace（已含 core-* crates 骨架）
  - [x] `crates/core-ble`
  - [x] `crates/core-atvv`
  - [x] `crates/core-audio`
  - [x] `crates/core-hid`
  - [x] `crates/core-input`
  - [x] `crates/core-mapping`
  - [x] `crates/core-config`
  - [ ] `crates/core-diagnostics`（诊断能力已并入 src-tauri commands + core-audio）
  - [ ] `crates/app`（由 src-tauri 承担）
- [x] 初始化 Tauri 2 + React + TypeScript 前端（前后端 ping 通）
- [x] 配置 `cargo fmt` / `clippy` / `rustfmt` 基础规范
- [x] 配置 GitHub Actions：Windows CI（core crates test + 前端 build + Tauri check）
- [x] 建立日志目录与统一错误上报结构（core-log）
- [x] 建立配置目录 `%LOCALAPPDATA%\RemoteMic\RC003`

## 2. 核心模块：BLE / ATVV

- [x] `core-ble`
  - [x] WinRT BLE 扫描 + 精确名称匹配（真机验证）
  - [x] GATT 枚举使用 `UNCACHED`（ATVV 服务 + TX/Audio/Control 特征发现）
  - [x] ATVV Service/Characteristic UUID 常量定义（在 core-atvv）
  - [x] 断开、自动重连、超时处理（bridge 重连循环）
  - [ ] 单实例 / 并发连接保护
  - [x] ATVV 传输层：订阅 Audio 特征 + Control 写入 + 事件回调（真机验证）
  - [x] 协议抓包记录器：Control/Audio 原始字节写入 captures 日志
- [x] `core-atvv`
  - [x] ATVV 能力协商（GET_CAPABILITIES 已发送，真机验证）
  - [x] MIC_OPEN / STREAM_START / STREAM_STOP 会话状态机（真机 AudioStarted/AudioStopped 验证）
  - [ ] MIC_EXTEND / 长时间语音租期处理
  - [x] 16kHz IMA/DVI ADPCM 解码（含合成码流 round-trip 测试）
  - [x] 同步包 / predictor / step index 重置
  - [x] 停止后 0.3 秒尾部数据丢弃策略

## 3. 核心模块：音频

- [x] `core-audio`
  - [x] WASAPI 音频端点枚举（真机验证）
  - [ ] 用户选择输出端点并持久化（当前固定 CABLE Input）
  - [x] 16kHz → 48kHz 重采样
  - [x] +10dB 增益 / 20Hz DC 阻挡
  - [x] 立体声端点声道复制（含限幅）
  - [x] 测试音（1kHz/1s/48k 立体声生成 + Windows cpal 播放到指定设备）
  - [x] 连续音频输出 Sink（cpal/WASAPI，队列推送）
  - [ ] 输出设备失配/断开时失败关闭
- [x] 虚拟声卡方案
  - [x] 检测未装 → 设置页「安装 VB-CABLE」按钮（官方包下载 + UAC 安装）
  - [x] 固定 VB-CABLE 官方包 SHA-256（已写入 scripts/vb-cable.sha256，含来源/日期）
  - [ ] 可选：随包提供官方 VB-CABLE + SHA-256 校验 + UAC 安装
  - [ ] 可选：评估自研虚拟音频驱动可行性

## 4. 核心模块：HID / 按键

- [x] `core-hid`
  - [x] Raw Input 捕获框架（真机验证）
  - [ ] HID 路径校验，避免同型号多设备串扰
  - [x] RC003 物理 usage 映射表 + Raw Input 报告解析（纯逻辑）
  - [x] Windows Raw Input 缺失键（返回 `0xF1`、音量 `0x80/0x81`、TV `0x35`）旁路方案（Frida Tap）
  - [x] Frida/WUDFHost 旁路可行性验证（驱动层拦截已落地）
  - [ ] 记录按键采集与回放工具
- [x] `core-input`
  - [x] `SendInput` 键盘事件注入
  - [x] 防止一次按键双触发（拦截单源 + 去重）
  - [x] 组合快捷键（Win 键、Ctrl、Alt 等）
  - [x] 打开应用动作（后端已支持，UI 待暴露）
  - [x] 系统音量 / 播放控制
- [x] `core-mapping`
  - [x] 13 键默认映射表（12 键单击 + 麦克风 Press/Release→Voice）
  - [x] 单击 / 双击 / 长按触发条件 + 按下/松开边沿触发（TriggerDetector）
  - [x] 长按只触发一次（已取消按住连发）
  - [x] 配置热加载（运行中修改映射立即生效）
  - [x] 长按阈值 / 双击窗口可设置并热更新
  - [ ] 自定义快捷键录制 UI

## 5. 核心模块：系统语音输入优先 / 第三方 IME 兼容

- [ ] `core-ime`
  - [x] Windows 自带语音输入（Win+H）作为基础链路，优先级最高
  - [x] 触发 `Win+H` 系统语音键入（SendInput，Windows）
  - [x] 引导用户把语音输入麦克风设为 `CABLE Output`（引导页）
  - [ ] 诊断：Windows 语音语言包 / 在线语音可用性 / 麦克风权限
  - [ ] 抽象 IME 接口（为后续第三方输入法预留）
  - [ ] 豆包输入法：`ralt` 按住 / `ralt+space` 切换（可选扩展）
  - [ ] 微信输入法兼容（可选扩展）
  - [ ] 注入事件被 IME 识别为真实按键的兼容层（第三方 IME 需要时再做）
  - [x] ATVV 字节 → ADPCM 解码 → 48k 立体声输出帧（core-voice 引擎）
  - [x] 模拟完整语音链（MIC_BUTTON→AUDIO_START→语音→AUDIO_STOP）

## 6. 配置 / 日志 / 诊断

- [x] `core-config`
  - [x] 原子化 JSON 写入（临时文件 + fsync + rename）
  - [x] 设置持久化：设备连接后自动保存、拦截开关、长按/双击时间
  - [x] 配置迁移 / 旧版兼容（Mic SingleClick → Press/Release 自动迁移）
  - [ ] 配置损坏时保留最后一份有效配置
- [ ] `core-diagnostics`
  - [ ] BLE 状态诊断
  - [x] 音频端点检测 + VB-CABLE 链路检查（诊断页已接入后端）
  - [ ] 按键采集向导
  - [ ] Raw Input 广域探针
  - [x] 能力自检（PASS/FAIL）+ Demo 按键写统计
- [x] 日志/抓包文件查看（诊断页）
  - [x] core-log 日志轮转（超过 2 MiB 自动轮转，保留 5 份）
  - [x] 诊断页：查看日志尾部 / 清空日志 / 打开日志目录 / 切换 DEBUG
  - [ ] 隐私边界：不记录蓝牙地址 / HID 路径 / 语音内容

## 7. UI（Tauri + React）

- [x] 连接页：设备信息与状态概览布局 + 拦截 HID 按键信号开关
- [x] 按键映射页：13 键布局 + 后端默认映射读取 + 按下/松开（仅麦克风）+ 长按/双击时间设置
- [x] 语音页：输出端点选择（接后端 WASAPI）+ 语音输入方式（Win+H）
- [x] 诊断页：检查项布局
- [ ] 权限/帮助页：系统权限跳转、FAQ
- [x] 引导页（配对 / 蓝牙权限 / 麦克风 / VB-CABLE / Win+H）
- [x] 引导页快捷按钮（打开 Windows 蓝牙/麦克风/声音设置）
- [x] 本地统计页（按键次数 / 语音时长）
- [ ] 系统托盘菜单
- [ ] 多语言（简体中文 / English）

## 8. 打包 / 签名 / 发布

- [x] Tauri Bundle 配置
  - [x] 安装器（NSIS，中文/English，currentUser）
  - [ ] 便携版 ZIP
  - [x] GitHub Actions 自动生成 SHA256SUMS.txt（随 Release 上传）
- [ ] 签名方案
  - [ ] 免费自签 Authenticode
  - [ ] SmartScreen 提示文档
  - [ ] 可选：公共 CA / EV 签名评估
- [ ] 自动更新方案（WinSparkle / 自更新 / 手动下载）
- [x] GitHub Actions 自动构建 Windows 安装包（tag / 手动触发）
  - [x] 打 tag 自动创建 GitHub Release 并上传 exe + SHA256
- [ ] 卸载后配置/日志保留策略说明

## 9. 测试与真机验收

- [x] Rust 单元测试：ATVV / ADPCM / 动作 / 配置 / 触发检测
- [ ] Rust 集成测试：BLE contract / 音频 contract / 按键链
- [ ] 前端测试：Vitest + Playwright
- [x] CI 全绿门禁（fmt / clippy -D warnings / test / check / 前端 build）
- [x] 真机验收清单（见 `docs/项目/真机验收.md`，部分完成）
  - [x] RC003 配对（重连/断线恢复待补测）
  - [x] 普通键单次触发、无双触发（方向/返回/音量/TV 已验；确定/主页/菜单/电源待补测）
  - [x] 长按（双击待补测；按住连发已取消）
  - [ ] Win+H 系统语音输入出字（基础验收）
  - [ ] 麦克风键 → 豆包输入法出字（可选扩展）
  - [ ] 微信输入法出字（可选扩展）
  - [ ] 长时间语音（>60 秒）
  - [ ] 安装器 / 便携版 / 升级 / 卸载

## 10. 里程碑追踪

- [x] M0：协议与基线复验完成
- [x] M1：Rust 核心骨架 + Tauri 壳完成
- [x] M2：按键链路真机通过
- [x] M3：语音链路真机通过（采集/解码/弹窗；出字待补测）
- [ ] M4：产品化（安装器、签名、诊断、文档）
- [ ] M5：扩展（RC001、更多第三方 IME、统计、自动更新）
